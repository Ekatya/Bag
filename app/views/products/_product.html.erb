<% @categories.each do |sub| %>
<% if @category_id.to_s==sub.id.to_s %>

 <<%= link_to sub.name, controller: "categories", action: "show", id: sub.id %>>

<% else %>

 <%= link_to sub.name, controller: "categories", action: "show", id: sub.id %>

<% end %>
<% end %>

<br> <br>

<!------------------------>


<% @subcategories.each do |sub| %>
<% if @subcategory_id.to_s==sub.id.to_s %>

 <<%= link_to sub.name, controller: "subcategories", action: "show", id: sub.id, id2: @category_id %>>

<% else %>

 <%= link_to sub.name, controller: "subcategories", action: "show", id: sub.id, id2: @category_id %>

<% end %>
<% end %>

<!------------------------>

<br> <br>
<% @products.each do |prod| %>
 <%= link_to prod.name, prod %>  <%= prod.category_id  %>  <%= prod.subcategory_id %><br>
<% end %>
<br> <br>
<%= will_paginate @products %>